---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="YoriTofu - Authentic Korean Restaurant" description="Experience the best Korean cuisine with sizzling stone pot dishes, bubbling stews, and fresh tofu specialties. Sizzling, Bubbling & Cheering at YoriTofu.">
	<Header />
	
	<main>
		<!-- Banner Slider Section -->
		<section class="banner-slider">
			<div class="slider-container">
				<div class="slides">
					<!-- Banner 1 -->
					<div class="slide active">
						<div class="banner-content">
							<div class="container">
								<div class="banner-text">
									<h1 class="banner-title">
										Late Nights, <span class="accent-text">Korean Vibes</span>
									</h1>
									<p class="banner-description">
										We're open late most nights<br>
										Perfect for dinner, drinks, and good company.
									</p>
									<div class="banner-actions">
										<a href="/menu" class="btn btn-primary btn-lg">Explore Menu</a>
									</div>
								</div>
							</div>
						</div>
						<div class="banner-background" style="background-image: url('/banner1.jpg');"></div>
					</div>

					<!-- Banner 2 -->
					<div class="slide">
						<div class="banner-content">
							<div class="container">
								<div class="banner-text">
									<h1 class="banner-title">
										Late Nights, <span class="accent-text">Korean Vibes</span>
									</h1>
									<p class="banner-description">
										We're open late most nights<br>
										Perfect for dinner, drinks, and good company.
									</p>
									<div class="banner-actions">
										<a href="/menu" class="btn btn-primary btn-lg">Explore Menu</a>
									</div>
								</div>
							</div>
						</div>
						<div class="banner-background" style="background-image: url('/banner2.jpg');"></div>
					</div>

					<!-- Banner 3 -->
					<div class="slide">
						<div class="banner-content">
							<div class="container">
								<div class="banner-text">
									<h1 class="banner-title">
										Late Nights, <span class="accent-text">Korean Vibes</span>
									</h1>
									<p class="banner-description">
										We're open late most nights<br>
										Perfect for dinner, drinks, and good company.
									</p>
									<div class="banner-actions">
										<a href="/menu" class="btn btn-primary btn-lg">Explore Menu</a>
									</div>
								</div>
							</div>
						</div>
						<div class="banner-background" style="background-image: url('/banner3.jpg');"></div>
					</div>

					<!-- Banner 4 -->
					<div class="slide">
						<div class="banner-content">
							<div class="container">
								<div class="banner-text">
									<h1 class="banner-title">
										Late Nights, <span class="accent-text">Korean Vibes</span>
									</h1>
									<p class="banner-description">
										We're open late most nights<br>
										Perfect for dinner, drinks, and good company.
									</p>
									<div class="banner-actions">
										<a href="/menu" class="btn btn-primary btn-lg">Explore Menu</a>
									</div>
								</div>
							</div>
						</div>
						<div class="banner-background" style="background-image: url('/banner4.jpg');"></div>
					</div>

					<!-- Banner 5 -->
					<div class="slide">
						<div class="banner-content">
							<div class="container">
								<div class="banner-text">
									<h1 class="banner-title">
										Late Nights, <span class="accent-text">Korean Vibes</span>
									</h1>
									<p class="banner-description">
										We're open late most nights<br>
										Perfect for dinner, drinks, and good company.
									</p>
									<div class="banner-actions">
										<a href="/menu" class="btn btn-primary btn-lg">Explore Menu</a>
									</div>
								</div>
							</div>
						</div>
						<div class="banner-background" style="background-image: url('/banner5.jpg');"></div>
					</div>
				</div>

				<!-- Navigation Dots -->
				<div class="slider-dots">
					<button class="dot active" data-slide="0"></button>
					<button class="dot" data-slide="1"></button>
					<button class="dot" data-slide="2"></button>
					<button class="dot" data-slide="3"></button>
					<button class="dot" data-slide="4"></button>
				</div>

				<!-- Navigation Arrows -->
				<button class="slider-arrow prev" aria-label="Previous slide">
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polyline points="15,18 9,12 15,6"></polyline>
					</svg>
				</button>
				<button class="slider-arrow next" aria-label="Next slide">
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polyline points="9,18 15,12 9,6"></polyline>
					</svg>
				</button>
			</div>
		</section>

		<!-- Menu Preview Section -->
		<section class="menu-preview section">
			<div class="container">
				<div class="section-header">
					<h2 class="section-title">Eat. Drink. Cheer!</h2>
					<p class="section-description">
						Bold Korean flavors made to share and celebrate.
					</p>
				</div>
				
				<div class="menu-categories">
					<div class="menu-category card">
						<div class="menu-category-image">
							<div class="image-placeholder">Korean Pub Bites</div>
						</div>
						<div class="card-body">
							<h3 class="menu-category-title">Korean Pub Bites</h3>
							<p class="menu-category-description">
								Perfect bites made to pair with your drink.
							</p>
							<a href="/menu#pub-bites" class="btn btn-outline">View Menu</a>
						</div>
					</div>
					
					<div class="menu-category card">
						<div class="menu-category-image">
							<div class="image-placeholder">Cheering Menu</div>
						</div>
						<div class="card-body">
							<h3 class="menu-category-title">Cheering Menu</h3>
							<p class="menu-category-description">
								Raise your glass, keep the vibe alive.
							</p>
							<a href="/drinks" class="btn btn-outline">View Drinks</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- About Preview Section -->
		<section class="about-preview section">
			<div class="container">
				<div class="about-content">
					<div class="about-text">
						<h2 class="section-title">About YoriTofu</h2>
						<p class="about-description">
							Welcome to YoriTofu, where every meal is a celebration of flavor and warmth. 
							We take pride in our variety of wholesome tofu dishes, crafted with care to 
							delight both vegetarians and anyone seeking a healthy, satisfying meal.
						</p>
						<p class="about-description">
							Every dish is made with fresh ingredients, balanced flavors, and a touch of 
							love—because great food is about more than just taste; it's about sharing 
							moments, creating memories, and feeling truly nourished.
						</p>
					</div>
					<div class="about-image">
						<img src="/store.jpeg" alt="YoriTofu Restaurant Interior" class="about-restaurant-image" />
					</div>
				</div>
			</div>
		</section>

		<!-- CTA Section -->
		<section class="cta section">
			<div class="container">
				<div class="cta-content">
					<h2 class="cta-title">Ready for an Authentic Korean Experience?</h2>
					<p class="cta-description">
						Join us for a memorable dining experience with fresh ingredients, 
						traditional recipes, and warm hospitality.
					</p>
					<div class="cta-actions">
						<a href="https://www.menoves-online.com/ordering/restaurant/menu?company_uid=c5ecb552-ab71-411d-98fb-f0056cc73a44&restaurant_uid=c420bc2d-c305-4b26-ab09-2baa1ba2c812&facebook=true" class="btn btn-primary btn-lg">Online Order</a>
						<a href="https://www.google.com/maps/place/Yoritofu/@33.837312,-118.3580216,16z/data=!3m1!4b1!4m6!3m5!1s0x80c2b555906b0d67:0xca80373d51c74171!8m2!3d33.837312!4d-118.3554467!16s%2Fg%2F11xyfjwdgb?entry=ttu&g_ep=EgoyMDI1MDkyNC4wIKXMDSoASAFQAw%3D%3D" class="btn btn-outline btn-lg">Get Directions</a>
					</div>
				</div>
			</div>
		</section>
	</main>
	
	<Footer />
</Layout>

<style>
	/* Banner Slider Section */
	.banner-slider {
		min-height: 100vh;
		position: relative;
		overflow: hidden;
	}

	.slider-container {
		position: relative;
		width: 100%;
		height: 100vh;
	}

	.slides {
		position: relative;
		width: 100%;
		height: 100%;
	}

	.slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0;
		visibility: hidden;
		transition: opacity 0.6s ease-in-out;
	}

	.slide.active {
		opacity: 1;
		visibility: visible;
	}

	.banner-content {
		position: relative;
		z-index: 2;
		display: flex;
		justify-content: center;
		align-items: center;
		height: 100%;
		text-align: center;
	}

	.banner-content::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			135deg,
			rgba(0, 0, 0, 0.4) 0%,
			rgba(0, 0, 0, 0.2) 50%,
			rgba(0, 0, 0, 0.6) 100%
		);
		z-index: -1;
	}

	.banner-text {
		max-width: 700px;
		padding: var(--space-8);
		background: rgba(255, 255, 255, 0.05);
		backdrop-filter: blur(10px);
		border-radius: var(--radius-2xl);
		border: 1px solid rgba(255, 255, 255, 0.1);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
	}

	.banner-title {
		font-size: clamp(2.5rem, 5vw, 4rem);
		font-weight: var(--font-weight-extrabold);
		line-height: 1.1;
		margin-bottom: var(--space-6);
		color: var(--color-white);
		text-shadow: 
			0 2px 4px rgba(0, 0, 0, 0.8),
			0 4px 8px rgba(0, 0, 0, 0.6),
			0 8px 16px rgba(0, 0, 0, 0.4);
		letter-spacing: -0.02em;
	}

	.accent-text {
		color: var(--color-accent);
		text-shadow: 
			0 2px 4px rgba(0, 0, 0, 0.8),
			0 4px 8px rgba(0, 0, 0, 0.6),
			0 0 20px rgba(255, 107, 53, 0.3);
		position: relative;
	}

	.accent-text::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, transparent, var(--color-accent), transparent);
		border-radius: 2px;
	}

	.banner-description {
		font-size: clamp(1.125rem, 2.5vw, 1.5rem);
		color: rgba(255, 255, 255, 0.95);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--space-8);
		text-shadow: 
			0 1px 2px rgba(0, 0, 0, 0.8),
			0 2px 4px rgba(0, 0, 0, 0.6);
		font-weight: var(--font-weight-medium);
	}

	.banner-actions {
		display: flex;
		gap: var(--space-4);
		flex-wrap: wrap;
		justify-content: center;
	}

	.banner-actions .btn {
		position: relative;
		padding: var(--space-4) var(--space-8);
		font-size: var(--font-size-lg);
		font-weight: var(--font-weight-semibold);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		border-radius: var(--radius-full);
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		backdrop-filter: blur(10px);
		border: 2px solid transparent;
		overflow: hidden;
	}

	.banner-actions .btn::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
		transition: left 0.5s;
	}

	.banner-actions .btn:hover::before {
		left: 100%;
	}

	.banner-actions .btn-primary {
		background: linear-gradient(135deg, var(--color-accent) 0%, #ff8a50 100%);
		color: var(--color-white);
		box-shadow: 
			0 4px 15px rgba(255, 107, 53, 0.4),
			0 8px 25px rgba(0, 0, 0, 0.3);
		border-color: var(--color-accent);
	}

	.banner-actions .btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 
			0 8px 25px rgba(255, 107, 53, 0.6),
			0 15px 35px rgba(0, 0, 0, 0.4);
		background: linear-gradient(135deg, #ff8a50 0%, var(--color-accent) 100%);
	}

	.banner-actions .btn-outline {
		background: rgba(255, 255, 255, 0.1);
		color: var(--color-white);
		border-color: rgba(255, 255, 255, 0.3);
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
	}

	.banner-actions .btn-outline:hover {
		background: rgba(255, 255, 255, 0.2);
		border-color: rgba(255, 255, 255, 0.5);
		transform: translateY(-2px);
		box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
	}

	.banner-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		z-index: 1;
	}

	/* Navigation Dots */
	.slider-dots {
		position: absolute;
		bottom: 30px;
		left: 50%;
		transform: translateX(-50%);
		display: flex;
		gap: 12px;
		z-index: 3;
	}

	.dot {
		width: 12px;
		height: 12px;
		border-radius: 50%;
		border: 2px solid rgba(255, 255, 255, 0.7);
		background-color: transparent;
		cursor: pointer;
		transition: all 0.3s ease;
	}

	.dot.active,
	.dot:hover {
		background-color: var(--color-white);
		border-color: var(--color-white);
		transform: scale(1.2);
	}

	/* Navigation Arrows */
	.slider-arrow {
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		width: 50px;
		height: 50px;
		background-color: rgba(0, 0, 0, 0.3);
		border: none;
		border-radius: 50%;
		color: var(--color-white);
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: all 0.3s ease;
		z-index: 3;
	}

	.slider-arrow:hover {
		background-color: rgba(0, 0, 0, 0.6);
		transform: translateY(-50%) scale(1.1);
	}

	.slider-arrow.prev {
		left: 20px;
	}

	.slider-arrow.next {
		right: 20px;
	}

	.slider-arrow svg {
		width: 20px;
		height: 20px;
	}
	
	.hero-placeholder,
	.image-placeholder {
		width: 100%;
		height: 100%;
		background-color: var(--color-gray-200);
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-gray-500);
		font-weight: var(--font-weight-medium);
		border-radius: var(--radius-lg);
	}
	
	.section-header {
		text-align: center;
		margin-bottom: var(--space-12);
	}
	
	.section-title {
		font-size: var(--font-size-3xl);
		font-weight: var(--font-weight-bold);
		color: var(--color-gray-900);
		margin-bottom: var(--space-4);
	}
	
	.section-description {
		font-size: var(--font-size-lg);
		color: var(--color-gray-600);
		max-width: 600px;
		margin: 0 auto;
	}
	
	/* About Preview Section */
	.about-preview {
		background-color: var(--color-gray-50);
	}
	
	.about-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--space-12);
		align-items: center;
	}
	
	.about-description {
		color: var(--color-gray-600);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--space-6);
	}
	
	.about-image {
		height: 400px;
	}
	
	.about-restaurant-image {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: var(--radius-lg);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
	}
	
	/* Menu Preview Section */
	.menu-preview {
		background-color: var(--color-white);
	}
	
	.menu-categories {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
		gap: var(--space-8);
	}
	
	.menu-category {
		transition: transform var(--transition-normal);
	}
	
	.menu-category:hover {
		transform: translateY(-4px);
	}
	
	.menu-category-image {
		height: 200px;
		margin-bottom: var(--space-4);
	}
	
	.menu-category-title {
		font-size: var(--font-size-xl);
		font-weight: var(--font-weight-semibold);
		color: var(--color-gray-900);
		margin-bottom: var(--space-3);
	}
	
	.menu-category-description {
		color: var(--color-gray-600);
		margin-bottom: var(--space-4);
	}
	
	/* CTA Section */
	.cta {
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
		color: var(--color-white);
		text-align: center;
	}
	
	.cta-content {
		max-width: 800px;
		margin: 0 auto;
	}
	
	.cta-title {
		font-size: var(--font-size-3xl);
		font-weight: var(--font-weight-bold);
		color: var(--color-white);
		margin-bottom: var(--space-4);
	}
	
	.cta-description {
		font-size: var(--font-size-lg);
		color: rgba(255, 255, 255, 0.9);
		line-height: var(--line-height-relaxed);
		margin-bottom: var(--space-8);
	}
	
	.cta-actions {
		display: flex;
		gap: var(--space-4);
		justify-content: center;
		flex-wrap: wrap;
	}
	
	.cta .btn-outline {
		border-color: var(--color-white);
		color: var(--color-white);
	}
	
	.cta .btn-outline:hover {
		background-color: var(--color-white);
		color: var(--color-primary);
	}
	
	/* Responsive Design */
	@media (max-width: 768px) {
		.banner-slider {
			min-height: 100vh;
		}

		.banner-text {
			max-width: 90%;
			padding: var(--space-6);
			margin: 0 var(--space-4);
		}

		.banner-title {
			font-size: clamp(2rem, 8vw, 3rem);
			margin-bottom: var(--space-4);
		}

		.banner-description {
			font-size: clamp(1rem, 4vw, 1.25rem);
			margin-bottom: var(--space-6);
		}

		.banner-actions {
			justify-content: center;
			gap: var(--space-3);
		}

		.banner-actions .btn {
			padding: var(--space-3) var(--space-6);
			font-size: var(--font-size-base);
			min-width: 140px;
		}

		/* Hide arrows on mobile, keep dots */
		.slider-arrow {
			display: none;
		}

		.slider-dots {
			bottom: 20px;
		}
		
		.about-content {
			grid-template-columns: 1fr;
			gap: var(--space-8);
		}
		
		.menu-categories {
			grid-template-columns: 1fr;
		}
		
		.cta-actions {
			flex-direction: column;
			align-items: center;
		}
		
		.cta-actions .btn {
			width: 100%;
			max-width: 300px;
		}
	}
	
	@media (max-width: 480px) {
		.banner-text {
			padding: var(--space-4);
			margin: 0 var(--space-2);
		}

		.banner-title {
			font-size: clamp(1.75rem, 10vw, 2.5rem);
			margin-bottom: var(--space-3);
		}

		.banner-description {
			font-size: clamp(0.875rem, 5vw, 1.125rem);
			margin-bottom: var(--space-4);
		}

		.banner-actions {
			flex-direction: column;
			align-items: center;
			gap: var(--space-2);
		}

		.banner-actions .btn {
			width: 100%;
			max-width: 200px;
			padding: var(--space-3) var(--space-4);
			font-size: var(--font-size-sm);
		}

		.accent-text::after {
			height: 2px;
			bottom: -2px;
		}

		.section-title {
			font-size: var(--font-size-2xl);
		}

		.cta-title {
			font-size: var(--font-size-2xl);
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', function() {
		const slides: NodeListOf<Element> = document.querySelectorAll('.slide');
		const dots: NodeListOf<Element> = document.querySelectorAll('.dot');
		const prevArrow: Element | null = document.querySelector('.slider-arrow.prev');
		const nextArrow: Element | null = document.querySelector('.slider-arrow.next');

		let currentSlide: number = 0;
		let slideInterval: number;

		// Initialize slider
		function initSlider() {
			showSlide(currentSlide);
			startAutoSlide();
		}

		// Show specific slide
		function showSlide(index: number) {
			// Hide all slides
			slides.forEach(slide => {
				slide.classList.remove('active');
			});

			// Remove active class from all dots
			dots.forEach(dot => {
				dot.classList.remove('active');
			});

			// Show current slide and activate corresponding dot
			slides[index].classList.add('active');
			dots[index].classList.add('active');

			currentSlide = index;
		}

		// Next slide
		function nextSlide() {
			currentSlide = (currentSlide + 1) % slides.length;
			showSlide(currentSlide);
			resetAutoSlide();
		}

		// Previous slide
		function prevSlide() {
			currentSlide = (currentSlide - 1 + slides.length) % slides.length;
			showSlide(currentSlide);
			resetAutoSlide();
		}

		// Go to specific slide
		function goToSlide(index: number) {
			showSlide(index);
			resetAutoSlide();
		}

		// Auto slide functionality
		function startAutoSlide() {
			slideInterval = window.setInterval(nextSlide, 5000); // Change slide every 5 seconds
		}

		function resetAutoSlide() {
			window.clearInterval(slideInterval);
			startAutoSlide();
		}

		// Event listeners
		if (nextArrow) {
			nextArrow.addEventListener('click', nextSlide);
		}

		if (prevArrow) {
			prevArrow.addEventListener('click', prevSlide);
		}

		// Dot click events
		dots.forEach((dot, index) => {
			dot.addEventListener('click', () => goToSlide(index));
		});

		// Pause auto slide on hover
		const sliderContainer: Element | null = document.querySelector('.slider-container');
		if (sliderContainer) {
			sliderContainer.addEventListener('mouseenter', () => {
				window.clearInterval(slideInterval);
			});

			sliderContainer.addEventListener('mouseleave', () => {
				startAutoSlide();
			});
		}

		// Initialize the slider
		initSlider();
	});
</script>
